# 机器人足球场虚拟平台 — 规格整理与构建计划

> 目标:在 mjlab 中构建一个**中型机器人足球场**虚拟平台,让 **Unitree G1 人形机器人**在指定矩形场地范围内移动。边界采用**软边界**(越界即重置 episode),场地绘制**完整球场线**。

---

## 一、统一约定(与你给的规格一致)

### 1.1 坐标系
| 项 | 定义 |
|---|---|
| 原点 | 场地几何中心 |
| 单位 | 米 (m) |
| x 轴 | 长度方向(左→右) |
| y 轴 | 宽度方向(下→上) |
| z 轴 | 竖直向上 |

### 1.2 线的定义
- 所有白线都是**有宽度的实体区域**,不是数学线。
- 线宽 `t = 0.125 m`,半线宽 `t/2 = 0.0625`。
- ⚠️ **关键基准**:所有尺寸以线的**外边缘**为准。场地总长 22m = 外边到外边。

### 1.3 半尺寸
- `L/2 = 11.0`(半长)
- `W/2 = 7.0`(半宽)
- 场地总尺寸 = **22 × 14 m**

---

## 二、完整尺寸表(右侧为准,左侧 x→-x 镜像)

| 元素 | 坐标范围 | 说明 |
|---|---|---|
| **外边界** | x∈[-11, 11], y∈[-7, 7] | 软边界判定基准 |
| **左边线** | x∈[-11, -10.875], y∈[-7, 7] | 白,矩形 |
| **右边线** | x∈[10.875, 11], y∈[-7, 7] | 白 |
| **下边线** | y∈[-7, -6.875], x∈[-11, 11] | 白 |
| **上边线** | y∈[6.875, 7], x∈[-11, 11] | 白 |
| **中线** | x∈[-0.0625, 0.0625], y∈[-7, 7] | 白 |
| **中心点** | 圆心(0,0), 半径 0.0625 | 实心圆 |
| **中圈** | 内径 1.9375, 外径 2.0625 | 圆环 (R_c=2.0) |
| **右小禁区·前线** | x∈[10.125, 10.25], y∈[-1.95, 1.95] | 白 |
| **右小禁区·上下边线** | y=±1.95(±t/2 扩展), x∈[10.25, 11] | 白,2 条 |
| **右大禁区·前线** | x∈[8.625, 8.75], y∈[-3.45, 3.45] | 白 |
| **右大禁区·上下边线** | y=±3.45(±t/2 扩展), x∈[8.75, 11] | 白,2 条 |
| **右罚球点** | (7.4, 0), 半径 0.10 | 实心圆 |
| **角球弧 ×4** | 圆心(±11, ±7), 内径 0.9375, 外径 1.0625 | 仅场内 90° 扇形 |
| **右球门** | 门线 x=11,内宽 y∈[-1,1],内高 z∈[0,1],深 x∈[11,11.5] | 3D,有碰撞 |
| **左球门** | 门线 x=-11(镜像) | 3D,有碰撞 |

### 球门结构(建议标准)
- 立柱直径 0.10(半径 0.05),横梁直径 0.10。
- 立柱:`(x=±11, y=±1)`,从 z=0 到 z=1,竖直 cylinder。
- 横梁:`(x=±11, z=1)`,沿 y 从 -1 到 +1,水平 cylinder。
- 深度结构:后立柱在 x=±11.5,顶部连接梁。
- 球门**必须有碰撞体**;球网仅可视化(无碰撞)。

### 颜色/材质
| 元素 | RGB |
|---|---|
| 场地 | (0, 130, 0) 绿 |
| 线 | (255,255,255) 白 |
| 左/右球门 | 蓝 / 黄(或统一白) |
| 球(若加) | 橙(高对比) |

---

## 三、mjlab 实现映射

mjlab 的地形/几何用 MuJoCo primitive 构建。各元素对应:

| 规格元素 | MuJoCo 实现 |
|---|---|
| 场地平面 (22×14) | 薄 `box` geom(绿),提供物理地面 |
| 直线(边线/中线/禁区线) | 薄扁 `box` geom(白),贴地、无碰撞 |
| 中心点/罚球点 | 薄 `cylinder` geom(白/实心圆) |
| 中圈/角球弧(曲线) | 一圈短 `box` 段拼接近似(分段数足够则视觉平滑) |
| 球门立柱/横梁 | `cylinder` geom(有碰撞) |
| 球网 | 半透明 box/视觉 site(无碰撞) |

**关于曲线**:MuJoCo 没有圆环/圆弧 primitive。中圈和角球弧会用 N 段短 box 拼成环/弧(例如中圈 64 段)。这是仿真里画曲线的标准做法,视觉上平滑,不影响物理(线无碰撞)。

### 坐标系映射(重要,避免"尺寸基准错"坑)
你的规格是**中心原点**坐标系。实现时所有 geom 的 `pos` 直接用你表格里的中心坐标,无需任何偏移换算。线的厚度方向(z)做成薄薄一层浮在地面上方约 1mm,避免与地面 z-fighting。

---

## 四、构建计划(分步)

> 开发规范(见 mjlab/CLAUDE.md):全程用 `uv run`;改完跑 `make check`(格式化+lint+类型检查),再跑相关测试。

### 步骤 1 — 足球场几何构建函数
新增 `src/mjlab/terrains/soccer_field.py`,实现一个函数 `build_soccer_field(spec, cfg)`:
- 输入:`MjSpec` 和一个 `SoccerFieldCfg`(含 L=22, W=14, 线宽 t=0.125, 禁区/中圈等尺寸,默认值即本文档表格)。
- 输出:在场地 body 上添加全部 geom(地面/线/球门),全部用中心原点坐标。
- 所有尺寸由 cfg 参数驱动,改尺寸只改一处。

### 步骤 2 — 软边界终止条件
在 velocity task 的 `mdp/terminations.py` 新增 `out_of_field_bounds(env, half_length, half_width, margin)`:
- 机器人根节点 xy 超出 `±(half_length - margin)` / `±(half_width - margin)` 即返回 True(截断 episode)。
- 默认 `half_length=11, half_width=7, margin=0.3`,即机器人保持在边线内活动。

### 步骤 3 — G1 足球场环境配置
基于现有 `unitree_g1_flat_env_cfg`(平地 G1 配置,见 `config/g1/env_cfgs.py`)派生 `unitree_g1_soccer_env_cfg`:
- 用足球场场地(通过 `SceneCfg.spec_fn` 回调注入步骤 1 的几何)替代纯 plane,保留 plane 作为物理地面兜底。
- reset 出生点 `pose_range` 限制在场内(如 x∈[-9,9], y∈[-5,5]),随机朝向。
- 终止条件加入步骤 2 的 `out_of_field_bounds`。
- 复用 velocity 任务现成的速度跟踪奖励/观测/动作(机器人按指令朝任意方向走/跑)。

### 步骤 4 — 注册任务
在 `config/g1/__init__.py` 用 `register_mjlab_task` 注册:
- `Mjlab-Soccer-Unitree-G1`(训练)
- `Mjlab-Soccer-Unitree-G1-Play`(可视化/回放,无随机扰动、无限回合)

### 步骤 5 — 验证
- `uv run python -m mjlab.scripts.list_envs` 确认任务已注册。
- 用 play 脚本加载场景,在 viewer 里目视检查:场地尺寸、线位置、球门位置、机器人出生与移动、越界重置是否生效。
- `make check` 通过。

---

## 五、已确认决策(2026-05-31)

1. **代码组织** → **路径 B**:基于 `unitree_g1_flat_env_cfg`(plane 地面)+ `SceneCfg.spec_fn` 回调,按中心坐标直接注入场地几何。
2. **外围挡板** → **不做**:纯软边界(越界重置),无物理矮墙。
3. **足球** → **现阶段不加**:只做场地 + G1 移动,球门几何保留。
4. **球门碰撞** → **保留碰撞体**:立柱/横梁有碰撞(规格要求),球网无碰撞。

--- git commit ---
a1e5e6d4bdc7a93f05cb9a4cd7af61c9c6a6a880


--- note ---
EXP16 (2026-06-09 22:19:34 run) was trained before the EXP17-19 OOB-fix arc.
Its source training dir: logs/rsl_rl/mos92_velocity/2026-06-09_22-19-34_spike_v4_e2e_ekf_kick
This mjlab.diff records the repo state at archival time (HEAD above).


--- git status ---
位于分支 main
您的分支领先 'origin/main' 共 35 个提交。
  （使用 "git push" 来发布您的本地提交）

要提交的变更：
  （使用 "git restore --staged <文件>..." 以取消暂存）
	删除：     soccer_eval/2026-06-09_v4/kick_oob_exp19/frame_0.png
	删除：     soccer_eval/2026-06-09_v4/kick_oob_exp19/frame_1.png
	删除：     soccer_eval/2026-06-09_v4/kick_oob_exp19/frame_2.png
	删除：     soccer_eval/2026-06-09_v4/kick_oob_exp19/frame_3.png
	删除：     soccer_eval/2026-06-09_v4/kick_oob_exp19/kick_oob_exp19-step-0.mp4

尚未暂存以备提交的变更：
  （使用 "git add <文件>..." 更新要提交的内容）
  （使用 "git restore <文件>..." 丢弃工作区的改动）
	修改：     scripts/eval_v4_kick_oob_exp19.py

未跟踪的文件:
  （使用 "git add <文件>..." 以包含要提交的内容）
	checkpoints/v4_soccer/kick_exp16/
	scripts/_v4_scenario_video.py
	soccer_eval/2026-06-09_v4/kick_oob_exp19/kick_oob_exp19_approach.mp4
	soccer_eval/2026-06-09_v4/kick_oob_exp19/kick_oob_exp19_approach.png
	soccer_eval/2026-06-09_v4/kick_oob_exp19/kick_oob_exp19_goalward.mp4
	soccer_eval/2026-06-09_v4/kick_oob_exp19/kick_oob_exp19_goalward.png
	soccer_eval/2026-06-09_v4/kick_oob_exp19/kick_oob_exp19_strike.mp4
	soccer_eval/2026-06-09_v4/kick_oob_exp19/kick_oob_exp19_strike.png
	soccer_eval/2026-06-09_v4/kick_oob_exp19/scenarios.json



--- git diff ---
diff --git a/scripts/eval_v4_kick_oob_exp19.py b/scripts/eval_v4_kick_oob_exp19.py
index 965d4eef..45d4343d 100644
--- a/scripts/eval_v4_kick_oob_exp19.py
+++ b/scripts/eval_v4_kick_oob_exp19.py
@@ -1,99 +1,40 @@
-"""EXP19 (representative deployment model) milestone video + stills.
+"""EXP19 (representative deployment model) — representative SCENARIO demo clips.
 
 Loads the archived EXP19 checkpoint (e2e EKF kick + out-of-bounds fix + strong
-soft-boundary shaping) and renders a rollout demo into
-soccer_eval/2026-06-09_v4/kick_oob_exp19/:
-  - kick_oob_exp19-step-0.mp4 : rollout video (9 envs tiled)
-  - frame_*.png               : preview stills sampled across the rollout
-
-Uses play=True (no GT-pose fade curriculum — the e2e EKF policy is pure belief,
-no oracle pose obs to fade). The quantitative metrics.json in this directory is
-the EXP16-19 multi-metric comparison (computed from the training-log steady
-state, four runs computed identically); this script only (re)generates the
-visual demo, so it does NOT overwrite metrics.json.
+soft-boundary shaping) and writes the three dribble-arc clips into
+soccer_eval/2026-06-09_v4/kick_oob_exp19/ via scripts/_v4_scenario_video.py:
+  - kick_oob_exp19_approach.mp4  : robot walking up to the ball
+  - kick_oob_exp19_strike.mp4    : the kick (peak ball speed)
+  - kick_oob_exp19_goalward.mp4  : ball driven toward the goal mouth (scored flag
+                                   in scenarios.json if a real goal was captured)
+plus one mid-clip still per scenario, and writes the picked windows to
+scenarios.json. The quantitative metrics.json (EXP16-19 comparison) is NOT
+touched.
 
 Usage: MUJOCO_GL=egl uv run python scripts/eval_v4_kick_oob_exp19.py
 """
 
-import os
+import json
 import sys
-from dataclasses import asdict
 from pathlib import Path
 
 sys.path.insert(0, str(Path(__file__).resolve().parent.parent))
 
-import mediapy as media
-import torch
+from _v4_scenario_video import run  # noqa: E402
 
-from mjlab.envs import ManagerBasedRlEnv
-from mjlab.rl import MjlabOnPolicyRunner, RslRlVecEnvWrapper
-from mjlab.tasks.velocity.config.mos92.env_cfgs import (
+from mjlab.tasks.velocity.config.mos92.env_cfgs import (  # noqa: E402
   mos92_soccer_e2e_dualcam_ekf_kick_oob_soft2_env_cfg,
 )
-from mjlab.tasks.velocity.config.mos92.rl_cfg import (
-  mos92_selfloc_vision_ppo_runner_cfg,
-)
-from mjlab.utils.torch import configure_torch_backends
-from mjlab.utils.wrappers.video_recorder import VideoRecorder
 
 CKPT = Path("checkpoints/v4_soccer/kick_oob_exp19/model_1999.pt")
 OUT_DIR = Path("soccer_eval/2026-06-09_v4/kick_oob_exp19")
-NAME_PREFIX = "kick_oob_exp19"
-VIDEO_NUM_ENVS = 9
-VIDEO_LEN = 500
-
-
-def _extract_frames(out_dir: Path) -> None:
-  mp4s = sorted(out_dir.glob("*.mp4"))
-  if not mp4s:
-    return
-  video = media.read_video(mp4s[0])
-  n = len(video)
-  idxs = [0, n // 3, 2 * n // 3, n - 1]
-  for j, fi in enumerate(idxs):
-    media.write_image(out_dir / f"frame_{j}.png", video[fi])
-
-
-def render_video(device: str) -> None:
-  env_cfg = mos92_soccer_e2e_dualcam_ekf_kick_oob_soft2_env_cfg(play=True)
-  env_cfg.scene.num_envs = VIDEO_NUM_ENVS
-  if "actor" in env_cfg.observations:
-    env_cfg.observations["actor"].enable_corruption = False
-  env_cfg.events.pop("push_robot", None)
-  agent_cfg = mos92_selfloc_vision_ppo_runner_cfg()
-  env = ManagerBasedRlEnv(cfg=env_cfg, device=device, render_mode="rgb_array")
-  env = VideoRecorder(
-    env,
-    video_folder=OUT_DIR,
-    step_trigger=lambda step: step == 0,
-    video_length=VIDEO_LEN,
-    name_prefix=NAME_PREFIX,
-    disable_logger=True,
-  )
-  env = RslRlVecEnvWrapper(env, clip_actions=agent_cfg.clip_actions)
-  runner = MjlabOnPolicyRunner(env, asdict(agent_cfg), str(OUT_DIR), device)
-  runner.load(str(CKPT), load_cfg={"actor": True}, strict=False, map_location=device)
-  policy = runner.get_inference_policy(device=device)
-  print(f"[INFO] rendering video from {CKPT.name}")
-  obs = env.get_observations()
-  for _ in range(VIDEO_LEN + 5):
-    with torch.no_grad():
-      actions = policy(obs)
-    obs, _, _, _ = env.step(actions)
-  env.close()
-  _extract_frames(OUT_DIR)
-  print(f"[INFO] video + frames saved -> {OUT_DIR}")
+PREFIX = "kick_oob_exp19"
 
 
 def main() -> None:
-  os.environ.setdefault("MUJOCO_GL", "egl")
-  configure_torch_backends()
-  OUT_DIR.mkdir(parents=True, exist_ok=True)
-  device = "cuda:0" if torch.cuda.is_available() else "cpu"
-  if not CKPT.exists():
-    print(f"[ERROR] checkpoint missing: {CKPT}")
-    return
-  render_video(device)
+  info = run(mos92_soccer_e2e_dualcam_ekf_kick_oob_soft2_env_cfg, CKPT, OUT_DIR, PREFIX)
+  (OUT_DIR / "scenarios.json").write_text(json.dumps(info, indent=2))
+  print(json.dumps(info, indent=2))
 
 
 if __name__ == "__main__":

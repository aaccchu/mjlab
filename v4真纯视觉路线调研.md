# v4 真纯视觉自定位推进路线调研

> 2026-06-10,12h 自主推进期间产出。目的:为去掉 EKF 自定位的两处 oracle(GT odometry、
> oracle 地标投影)找对方向、补齐漏洞。**只调研不动手**;实施前再按本文路线图逐阶段验证。
> 现状:策略输入=深度64x48(找球CNN)+RGB 96x72 6帧栈(自定位CNN)+本体感知;
> EkfPoseBelief 的 predict 用 GT odometry delta、地标"检测"是真实相机位姿的几何投影(完美检测器)。

## 一、RoboCup 真实机器人自定位的标准做法

**结构共识:轻量检测网络 → 场线/交点特征 → 概率滤波器。没有一家用"端到端像素→位姿"。**

1. **[B-Human 2023 code release]** SPL 标杆实现:多假设 UKF + 粒子滤波管理假设(仅 **12 个粒子**,Nao CPU 实时),观测为场线、中圈、罚球点及其组合特征(线交点、中圈+中线);感知-地图关联用"长度/角度/距离最合理的场线"+ 阈值门控,**关联成功率本身作为 reliability 喂给重采样**。(docs.b-human.de/coderelease2023 的 Self-Localization 与 Localization Features 章节)
2. **[YOEO / Hamburg Bit-Bots 2021]** 单共享 encoder 同时做目标检测(球/门柱)+ 语义分割(场线),嵌入式实时;场线分割经 IMU 辅助的逆透视映射落到地面,再喂粒子滤波。开源:github.com/bit-bots/YOEO。另有 [Sweaty FCNN 2018]:heatmap 输出关键点,比直接回归坐标更稳——**对我们 46 维直接回归头(EXP5f)是个重要提示**。
3. **[Iterative Landmark Matching / ARTEMIS 2025]**(arxiv 2503.11020)用 L/T/X 交点+门柱做迭代地标匹配 + 离群点剔除,显式处理误检,可不依赖 MCL。

反面参照:**[DeepMind vision soccer / Tirumala et al. 2024]**(arxiv 2405.02425)证明端到端 egocentric RGB + memory 可行,但代价是 NeRF 级渲染随机化 + 海量算力;我们 `soccer_robot_v4_localization_method.md` 已选定的 EKF 路线与 RoboCup 共识一致,无需推翻。

## 二、Odometry 精度量级与 RL 加噪标准

- 纯本体(IMU+腿部运动学)里程计漂移与步态强相关:**[Cerberus / Yang et al. 2023]**(arxiv 2209.07654)报告 VILO 经标定可压到 **<1% 行程**,并称纯 kinematic-inertial 基线明显更差(数 %);**[Buchanan et al. 2022]**(arxiv 2111.00789)learned IMU displacement 把相对位姿误差降 37%。按人形 0.3-0.5 m/s 步速折算,**2-5% 漂移 ≈ 0.4-1.5 m/min,yaw 漂移(陀螺 bias)是主要杀手**。
- RL 仿真加噪标准做法(Isaac Lab `NoiseModelWithAdditiveBiasCfg` 模式):**每步白噪声(高斯,σ 正比位移量)+ reset 时采样的恒定 bias**;要模拟真漂移需自定义 **bias random walk / OU 过程**(每步 bias += 小高斯增量)。对 SE2 delta 的推荐模型:`noisy_delta = delta * (1+scale_err) + N(0,σ_xy·‖delta‖) + bias_t`,yaw 同理且 bias 走随机游走。

## 三、地标检测训练法与架构兼容性

| 方案 | 与现架构兼容性 | 依据 |
|---|---|---|
| **在线渲染监督(aux loss)** | **最好**。`keypoint_uv_label`(observations.py:294)+ `KeypointAuxPPO`(src/mjlab/rl/keypoint_aux_ppo.py:23)管线已存在,标签每帧免费 | [Coarse-to-Fine 2021] 证明 privileged 渲染监督是 sim 内标准做法 |
| 预训练检测器冻结 | 中。需独立数据采集+训练循环,但隔离最彻底 | [KOVIS 2020]、[GeoLoco 2026] 冻结视觉先验避免表征崩塌 |
| teacher 蒸馏 | 低优先。解多技能冲突(线B)更合适,不是检测问题的对症药 | [DeepMind 2023, arxiv 2304.13653] |

**关键修正:EXP5f 的失败不在监督信号,在输出挂载点。** 检测头当年作为 action(env_cfgs.py:1215 `SelfLocActionCfg(dim=46)`,actions.py:395)挂在共享 trunk 上,违反了后来 EXP6 确立的"感知作 obs 不进 action"纪律。正确形态:**独立 detector 模块(可复用 RGB CNN 结构),用 keypoint_uv_label 监督训练,输出 (uv, conf) 喂给 `_belief_from_uv`(observations.py:377,docstring 已预留"Reused by oracle and (later) the learned detector")→ EKF。梯度只进 detector,与 PPO 完全解耦。** 输出层建议 heatmap+soft-argmax(Sweaty 经验),visibility 用显式 conf 头,conf 直接调制 EKF 的 R(observations.py:744 已有 `R/w` 加权,接口现成)。

## 四、分阶段路线图(每阶段一个变量,遵循分层验证纪律)

**阶段1:odometry 去 GT(改动最小,先做)**
- 改:`EkfPoseBelief.__call__` 的 predict 步(observations.py:711-723),GT delta → 噪声 delta(白噪+yaw bias 随机游走,课程化 σ);新增 env cfg(仿 env_cfgs.py:1454 派生)。Q(observations.py:680)须随 σ 同步调大。
- 判据:扫 σ∈{1%,3%,5%行程} 画 `Metrics/selfloc_pos_err_m` 曲线;3% 档 pos_err ≤1.2m(EXP13 oracle 基线 0.98m 的 ~1.25 倍)且 goal_rate/fell_over 不退。
- 风险:Q 与真实噪声不匹配 → 滤波器过度自信发散;bias 随机游走忘了 reset 清零 → 跨 episode 污染(对照 reset NaN-anchor 的既有处理 observations.py:697)。

**阶段2:真实关键点检测 CNN 替换 oracle 投影(主战役)**
- 2a 离线先行(仿 scripts/exp12_offline_ekf.py 纪律):录 rollout 帧+label,离线训 detector,报 kp_pixel_err / vis precision-recall;门槛:可见点像素误差 <2px(96 宽),vis F1>0.8。
- 2b 在线:`_collect_frame`(observations.py:504-520)中 `project_keypoints` 真值 → detector 推理输出;label 管线复用 env_cfgs.py:1254 的 `keypoint_label` 组,训练循环复用 KeypointAuxPPO 骨架但作用于独立模块参数。
- 判据:learned-detection EKF pos_err ≤2×oracle(≤2m),fell_over 与 EXP17 持平(感知零梯度入 trunk 的硬验证)。
- 风险:96×72 分辨率下 0.125m 场线可能不可检(v3g 已证单帧物理看不全)——离线 2a 就是为在烧 GPU 前回答这个问题;若 vis recall 极低,先升分辨率或只检 L/T/X 大特征(RoboCup 经验:交点比线更稳)。

**阶段3:ball-belief 显式化 + 对称消歧(收尾)**
- 改:仿 EkfPoseBelief 增 ball belief 项(pos/vel/conf/age),age 增长时 conf 衰减,解决近脚盲区 stale memory(codex:96.1% contact 窗口被漏);自定位侧引入镜像双假设(localization_method.md 方法2 CLAP)。
- 判据:contact 窗口 belief 命中率 >50%;对称跳变次数→0。

## 五、三大失败模式警示

1. **感知梯度再次污染 trunk**(EXP1-5f 教训,fell_over 1→37→52):任何把 detector 输出/损失接回 actor 共享参数的捷径都会复发。红线:detector 前向在 obs 管线内、`detach()` 后入 EKF,参数用独立 optimizer。
2. **EKF 在"噪声里程计 + 误检"双重打击下发散**:oracle 检测无误检,learned detector 必有 false positive,一个 3m 外的误关联就能把 mu 拽飞。必须加 Mahalanobis innovation 门控(B-Human 的阈值门控同源),且阶段1/阶段2 严禁同时上——这正是分层验证纪律的用武之地。
3. **conf 头的 reward-gaming 变体**:EXP5c 已演过"看不见=免费"(kp_visible 4→0.06);监督学习下对应物是 detector 学会全部输出低 conf 逃避像素损失。损失须对 vis 分类项独立加权,且监控 vis recall 而非只看 pixel err。

## 引用
B-Human Self-Localization / Localization Features (docs.b-human.de/coderelease2023) · YOEO (github.com/bit-bots/YOEO) · Sweaty FCNN 2018 · Iterative Landmark Matching (arxiv 2503.11020) · DeepMind vision soccer (arxiv 2405.02425) · Cerberus VILO (arxiv 2209.07654) · Buchanan learned inertial odometry (arxiv 2111.00789) · Pronto (frontiersin 2020) · Isaac Lab noise models · KOVIS (arxiv 2007.13960) · Coarse-to-Fine sim-to-real (arxiv 2105.11283)

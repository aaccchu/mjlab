# v4 评估指标详解

> 2026-06-09 创建,同日更新(新增真实射门率/ball_speed_peak/target_is_goal + 代码审查结论)。本文件解释 v4 训练/评估中**用到的所有指标**:它们怎么算、阈值多少、好坏方向、为什么重要。
> 指标定义见 `scripts/readout_v4_metrics.py`,计算逻辑见 `src/mjlab/tasks/velocity/mdp/dribble_command.py`。
> 判读工具按三个维度分组打印:**自定位 / 踢球链 / 稳定性**。每个指标报 mean / median / min / max / n(采样点数)。

---

## 读数说明

- **采样点**:每个数来自训练日志的一次记录(通常每若干 iteration 记一次)。"末 100"指取最近 100 个记录点统计,反映当前水平而非全程平均。
- **mean vs median**:median(中位数)对偶发极端值更稳健。两者差距大说明波动剧烈。
- **方向**:每个指标标注 ↑越高越好 或 ↓越低越好。
- **护栏指标**:线B(踢球)训练时,自定位和稳定性指标当"护栏"用——它们只要**不退化**就算合格,不要求提升。

---

## 一、自定位 LOCALIZATION(线A 核心)

### `pos_err_m`(selfloc_pos_err_m) ↓越低越好
- **含义**:EKF 算出的机器人位置与真实位置(GT)的欧氏距离,单位米。
- **算法**:`sqrt((x_est - x_gt)² + (y_est - y_gt)²)`,对所有并行环境取平均。
- **目标**:< 0.8m。当前 EXP13/14 ≈ **0.98m**。
- **重要性**:线A 的命门指标。它决定机器人"知不知道自己在哪",直接影响能否朝正确方向带球射门。
- **注意**:评估时需 **warmup ~150 步**让 EKF 收敛后再统计,否则虚高(EKF 初始从宽先验起步)。

### `uniq_frac`(scan_uniq_frac) ↑越高越好
- **含义**:一段时间窗口内,23 个场地地标中**去重后总共看到了几个**的比例(0~1)。
- **算法**:窗口内每个地标"是否被看到过"取或,求和除以 23。
- **当前**:≈ 0.17(约 4/23)。
- **重要性**:衡量"视野信息量"。它低,说明机器人视野窄、看到的地标少,EKF/Kabsch 缺约束。这是主动视觉(让机器人转头多看)想提升的量。0.17 偏低,但 EKF 靠跨帧累积部分弥补了它。

---

## 二、踢球链 KICK-CHAIN(线B 核心,最终成品能力)

### `goal_rate`(全局进球率) ↑越高越好
- **含义**:把球真正踢**进球门**的 episode 比例(0~1),**在全部 episode 上统计**。
- **算法**:球进入球门区域(局部坐标 `goal_line_x-0.2 < x < goal_line_x+0.6` 且 `|y| < goal_half_width`)即记一次"进球",每 episode 只记一次(0→1 跳变)。
- **当前**:EXP14 ≈ 0.11,EXP16 ≈ 0.14。
- **⚠️ 关键认知(2026-06-09 发现)**:**`goal_target_fraction=0.5`,只有一半 episode 的目标是球门**,另一半是"球周围 1-3m 的随机点"(纯带球任务,跟球门无关)。非球门 episode 的 goal_rate 几乎必然是 0,所以**全局 goal_rate 的理论上限被压到 ~0.5**,而不是 1.0。直接拿全局 goal_rate 当目标会被这一半无关 episode 稀释、难以解读。**已弃用全局 goal_rate 当主目标,改用下面的"真实射门率"。**

### `tgt_is_goal`(target_is_goal,目标是球门的比例) — 诊断用
- **含义**:被统计的 episode 中,目标恰好是球门(而非随机点)的比例。
- **算法**:每 episode resample 时按 `goal_target_fraction` 随机决定,latch 到 episode 末。
- **当前**:≈ 0.48~0.50(符合 goal_target_fraction=0.5)。
- **重要性**:它本身不是能力指标,而是**计算"真实射门率"的分母**。

### ★ 真实射门率(球门子集进球率) ↑越高越好 ★★最终主目标
- **含义**:**在"目标是球门"的那些 episode 里**,进球的比例。这才是真正的"会不会射门"。
- **算法**:`mean(goal_rate) / mean(target_is_goal)`(由 readout 工具自动计算并打印)。等价于"goals per goal-target episode"。
- **目标**:**子集 0.5~0.6**(对应全局 goal_rate ≈ 0.25~0.3)。
- **当前**:EXP16 ≈ **0.29** 并在上升(早期 0.23)。
- **重要性**:**整个 v4 的终极目标指标**,取代了被稀释的全局 goal_rate。0.2 全局 ≈ 0.4 子集,作为中期目标合理,但最终要到子集 0.5-0.6。
- **统计说明**:这是"比值的均值之比"(ratio of means),理论上小批量 reset 有微偏差,但训练用 `init_at_random_ep_len` 错开 reset、批量大,偏差可忽略;作为辅助判读足够准。
- **物理约束**:episode 仅 20s(1000 步@50Hz),球门在 11m 外,机器人随机生成平均离门 6-8m,部分球门 episode **物理上来不及**接近+带球进门 → 这进一步压低了射门率的可达上限,解读时要考虑。

### `episode_success`(回合成功率) ↑越高越好
- **含义**:一个 episode 中,球是否**曾经到达目标点 0.5m 范围内**(0~1)。
- **算法**:`ball_to_target_error < success_threshold(0.5m)`,episode 内取历史最大(到过一次就算成功)。
- **当前**:≈ 0.40。
- **⚠️ 不纯**:这个指标**混合了两种目标**——"到随机点"和"进球门",无法单独反映踢球能力。它高但射门率低,说明"球能带到目标附近(尤其随机点容易),但球门临门一脚没踢进"。解读时记住它含了容易的随机点任务。

### `episode_success`(回合成功率) ↑越高越好
- **含义**:一个 episode 中,球是否**曾经到达目标点 0.5m 范围内**(0~1)。
- **算法**:`ball_to_target_error < success_threshold(0.5m)`,episode 内取历史最大(到过一次就算成功)。
- **当前**:≈ 0.39。
- **重要性**:比 goal_rate 宽松的"接近成功"指标。它高但 goal_rate 低,说明"球能带到目标附近,但临门一脚没踢进"——这正是当前卡点。

### `ball_to_tgt_err`(ball_to_target_error,球-目标距离) ↓越低越好
- **含义**:球当前位置到目标点的距离,单位米。
- **算法**:球 xy 与目标 xy 的欧氏距离,实时值。
- **当前**:≈ 2.6m,且在训练中持续下降(2.97→2.65)。
- **重要性**:衡量"球被带得离目标多近"。它持续降而 goal_rate 不涨,是"会接近、不会终结"的直接证据。

### `robot_to_ball`(robot_to_ball_error,机器人-球距离) ↓越低越好
- **含义**:机器人到球的距离,单位米。
- **重要性**:衡量"机器人能否贴近球"。太大说明追不上球;本项目稳定在 ~0.65m,说明追球没问题,瓶颈在踢不在追。

### `possession`(占球步数) ↑越高越好(需结合其他指标看)
- **含义**:一个 episode 中机器人"贴着球"(robot_to_ball < 0.5m)的累计步数。
- **重要性**:衡量"控球时间"。但要警惕:占球高 + 进球低 = 可能在"抱着球不射门"。需和 ball_speed、ball_stuck 一起看。

### `ball_path`(ball_path_length,球行进总路程) ↑通常越高越好
- **含义**:一个 episode 中球累计移动的路程,单位米。
- **重要性**:球被踢动得多说明机器人在积极推进。但路程长 + 离目标远 = 乱踢;要结合 ball_to_tgt_err 看是否朝目标推进。

### `ball_speed`(球速,均值) — 适中为好
- **含义**:球的世界坐标 xy 速度,单位 m/s,**每步瞬时值**(日志里是该统计窗口的均值)。
- **重要性**:核心"球动没动"信号。太低=粘球/不踢;过高也不一定好(踢飞)。是下面防作弊指标的基础。
- **⚠️ 均值的盲区**:它对一个 episode 的所有步取平均,**含大量静止/慢走的步**。所以"一直慢推球"(全程 0.4)和"偶尔一脚踢出 2.5 + 大量静止"**读数都是 ~0.4,区分不出来**。这正是 EXP14/15 卡在 0.39 时无法判断"是不会踢还是不持续踢"的原因。→ 用下面的 `ball_speed_peak` 区分。

### `ball_speed_peak`(episode 峰值球速) ↑越高越好(诊断"会不会踢")
- **含义**:一个 episode 内球速的**峰值**(running max),单位 m/s。episode reset 时清零。
- **算法**:`ball_speed_peak = max(ball_speed_peak, ball_speed)` 每步更新。
- **当前**:EXP16 ≈ **2.5~2.9**。
- **重要性(2026-06-09 新增)**:**区分"慢推"和"真踢"的关键指标**。均值 ball_speed 掩盖了爆发力,峰值不会。EXP16 峰值达 2.85 而均值仅 0.36 → 直接证明**策略完全有能力踢出 2.5+m/s 的强球,只是不持续/不瞄准**——瓶颈是"踢得不稳定"而非"不会踢"。这个洞察直接指导了奖励设计方向(强化持续性而非爆发力)。

### `ball_stuck_s`(ball_stuck_time,粘球时长) ↓越低越好【防作弊】
- **含义**:球几乎静止(ball_speed < 0.05 m/s)的累计秒数。
- **重要性**:**反作弊指标**。机器人可能学会"压着球不动"骗占球奖励。这个值高=球被卡住没在推进,是坏信号。

> 另有 `holding_time`(near<0.4m 且 slow<0.2m/s 的"占有"时长)等内部信号用于触发 reset 和反作弊,不在主判读表里。

---

## 三、稳定性 STABILITY(护栏)

### `fell_over`(摔倒率) ↓越低越好 ★关键护栏
- **含义**:机器人摔倒导致 episode 终止的比例。
- **重要性**:**最重要的护栏**。v4 六轮失败的核心症状就是摔倒暴涨(EXP5f 达 52)。EXP6 修复后降到 ~0,之后任何训练都必须守住它不反弹。当前 ≈ 0.05,健康。

### `out_of_bounds`(out_of_field_bounds,出界率) ↓越低越好
- **含义**:机器人 **root(骨盆)** 越出场地边界导致 episode 终止的比例。**只看机器人,不看球**(terminations.py:71-89:判据是 `|root_x|>half_length-margin` 或 `|root_y|>half_width-margin`)。早期"球弹飞出界"是误判,已纠正。
- **重要性**:次要护栏,但**它的升降必须结合射门能力一起读**——出界与进球强相关(EXP16 实测 `out_bnd vs goal_rate +0.514`),因为学会朝球门(+x)大力射门会让身体前冲越过出界线。孤立地看"出界涨了=坏"会误判:它可能是"学会射门"的副产品。
- **⚠️ 关键认知(2026-06-09,EXP16 深挖,bug 级根因)**:EXP16 出界涨到 0.345 **不是调权重能解决的,而是一个 `time_out` 语义错误**——出界被误标 `time_out=True`,被 PPO 当"时间截断"bootstrap(`rewards += γ·V(出界后)`),等于明示"出界免罚"。完整因果链见本文件「六、out_of_bounds 升高的完整因果链」与 `v4越界处理调研.md`。EXP17 已修(`time_out=False` + 一次性惩罚 + 球门走廊)。
- **判读经验**:
  - `out_bnd 涨 + goal_rate/ball_speed_peak 也涨` → 大概率是"学会射门冲出界"的副产品(EXP16 状态),根因在 time_out 语义,不是策略乱跑。
  - `out_bnd 涨 + goal_rate 平/降 + robot_to_ball 涨` → 策略真的在乱跑/追不回球,是坏信号。
  - 修复后(EXP17)期望:`out_bnd 降向 ≤0.2` 而 `射门率守住 ~0.32`;若射门率反而塌、robot_to_ball 升,说明惩罚过重/走廊过窄(研究红线),需回退权重。

---

## 四、怎么综合判读(实战经验)

1. **先看护栏**:fell_over 和 pos_err 没退化,才有资格谈踢球进步。护栏破了,其他都是虚的。
2. **主目标看"真实射门率"**(goal_rate/target_is_goal),不是全局 goal_rate(被一半随机点 episode 稀释)。其他踢球指标都是它的"过程解释"。
3. **诊断卡点用组合**:
   - `射门率低 + ball_speed_peak 高` → **会踢但不稳定/不瞄准**(EXP16 状态:峰值 2.85 证明有踢力,均值低说明不持续)。这是当前主攻方向。
   - `射门率低 + ball_speed_peak 也低` → 真的不会踢(缺爆发动作,需重开探索/参考动作)。
   - `episode_success 高 + 射门率低` → 会到随机点(容易),不会进球门(难);注意 episode_success 含了简单的随机点任务。
   - `ball_to_tgt_err 降 + 射门率平` → 带球方向对,临门一脚缺。
   - `possession 高 + ball_stuck 高 + 射门率低` → 抱球作弊,reward 需修。
   - `robot_to_ball 大` → 连球都追不上(本项目未出现)。
4. **mean 和 median 一起看**:差距大说明个别环境拖累或暴走,需看 min/max 找极端情况。
5. **多 seed 防假阳性**(codex 血泪):别被单次/单 seed 的高射门率骗,要看 mean 稳定性 + 同时盯 fell_over。

---

## 附:指标-日志键名对照表

| 判读名 | 日志键名 | 维度 | 方向 |
|---|---|---|---|
| pos_err_m | Metrics/selfloc_pos_err_m | 定位 | ↓ |
| uniq_frac | Metrics/scan_uniq_frac | 定位 | ↑ |
| goal_rate | Metrics/dribble/goal_rate | 踢球 | ↑(被稀释,见正文) |
| tgt_is_goal | Metrics/dribble/target_is_goal | 踢球 | 诊断分母 |
| **真实射门率** | goal_rate/target_is_goal(readout 计算) | 踢球 | ↑ ★★ |
| episode_success | Metrics/dribble/episode_success | 踢球 | ↑(不纯) |
| possession | Metrics/dribble/possession | 踢球 | ↑* |
| ball_path | Metrics/dribble/ball_path_length | 踢球 | ↑* |
| ball_speed | Metrics/dribble/ball_speed | 踢球 | 适中(均值有盲区) |
| ball_speed_peak | Metrics/dribble/ball_speed_peak | 踢球 | ↑(诊断会不会踢) |
| ball_to_tgt_err | Metrics/dribble/ball_to_target_error | 踢球 | ↓ |
| ball_stuck_s | Metrics/dribble/ball_stuck_time | 踢球 | ↓ |
| robot_to_ball | Metrics/dribble/robot_to_ball_error | 踢球 | ↓ |
| fell_over | Episode_Termination/fell_over | 稳定 | ↓ ★ |
| out_of_bounds | Episode_Termination/out_of_field_bounds | 稳定 | ↓ |

(★=关键指标;★★=最终主目标;*=需结合其他指标判断,不能孤立看)

---

## 五、指标代码审查结论(2026-06-09)

对指标计算与判读代码做了系统审查(`dribble_command.py` 的 metrics 维护 + `readout_v4_metrics.py`),**未发现真 bug**:

- **reset 完整性 ✅**:基类 `CommandTerm.reset()` 先记录 episode 末值(`mean(metric[env_ids])`)再清零所有 metric,然后才调 `_resample_command`。所以 latch 型指标(goal_rate/episode_success/target_is_goal)记录的是 episode 最终值,累加型(possession/ball_path/peak 等)每 episode 从 0 重新累积。子类里的清零是无害冗余。
- **新指标正确 ✅**:`target_is_goal` 清零后正确重设;`ball_speed_peak` 每 episode 从 0 累积峰值,清零时机对。
- **readout 正则无误匹配 ✅**:每个 key 后强制紧跟 `:`,`ball_speed:` 与 `ball_speed_peak:` 天然区分(实测每 key 唯一匹配)。
- **射门率除法 ✅**:同批 env 取均值再相除,统计正确;有除零保护(`tgt>1e-6`)。仅"比值的均值之比"在极小批量有微偏差,当前 reset 模式下可忽略。
- **reward 依赖 ✅**:reward 消费的指标字段(at_goal/newly_scored/goal_scored/ball_speed/holding_time/ball_stuck_time)均为已有正确维护的字段,新指标未破坏任何 reward。

---

## 六、out_of_bounds 升高的完整因果链(2026-06-09 EXP16 深挖,bug 级根因)

> 起因:用户在 EXP16 训练中让我"想想机器人姿势变化、out_of_bounds 为什么增大"。沿这个方向挖出的不是调参问题,而是一个**代码 + 数据 + 文献三方钉死的 `time_out` 语义错误**。背景调研(FIFA/RoboCup 规则 + RL 边界处理菜单)存档在 `v4越界处理调研.md`;本节是供判读用的因果链与判断依据。

### 6.1 现象
- EXP16:`out_of_bounds 0.28(EXP14)→0.345`,与 `ball_speed_peak 2.57→2.94`、`robot_to_ball 0.75→0.92`、`ball_stuck_s ↑0.82` 同步上升。
- 同期主目标在涨:真实射门率 0.22→0.32(+45%)、fell_over 反而降到 0.044(最稳)。即"出界涨"与"变强"同时发生。

### 6.2 完整因果链(每一环都已验证)
```
std 重开探索(0.85)+ kick speed_threshold 降到 0.3
        │  学会朝球门(+x)大力射门(ball_speed_peak 2.57→2.94)
        ▼
射门时身体前冲(用户观察到的"姿势变化")→ root 越过出界线 x=10.7
        │  (out_bnd vs goal_rate 相关 +0.514 强;vs target_is_goal +0.242;vs possession −0.292)
        ▼
出界被判定 → 但 out_of_field_bounds 设了 time_out=True
        │  (env_cfgs.py:425;vecenv_wrapper.py:83 把 time_out 项塞进 extras["time_outs"])
        ▼
PPO 对 time_outs 的 step 执行 bootstrap:rewards += γ · V(出界后状态)
        │  (rsl_rl/algorithms/ppo.py:150-155)
        ▼
等于明示"出界后的世界和正常继续一样有价值" → 机器人完全不在乎出界
        ▼
out_of_bounds 随射门能力一起涨到 0.345(无收脚动机)
```

### 6.3 判断依据:为什么断定是语义错误而非调参问题
1. **代码**:出界 `time_out=True` 经 PPO bootstrap = 出界免罚。这是机制性的,不是权重没调好。
2. **数据**:`out_bnd vs goal_rate +0.514`(进球越多越出界)、`vs possession −0.292`(出界 episode 不是抱球磨蹭)→ 出界是"积极射门"的副产品,不是"消极乱跑"。
3. **文献**:Pardo 2017(arXiv:1712.00378)、Gymnasium 标准——`time_out`(truncation,该 bootstrap)只应给**纯时间到点**;越界是**任务失败**,应是 `terminated`(不 bootstrap)。无争议。
4. **对照(最有力)**:同一份配置里,**摔倒 fell_over 正确地设了 `time_out=False`**(当失败处理),唯独出界误设成 `True`。两者都是"机器人进了坏状态",却被区别对待 → 出界是孤立的误标。

### 6.4 修复(EXP17,研究第 1+2 步一起上)
- **第 1 步(根因)**:`out_of_field_bounds` 改 `time_out=False`(进 terminated 桶,不 bootstrap)+ 一次性 `out_of_bounds_penalty`(weight −5,dt 缩放后约 0.10,≈ goal_scored one-shot 的小比例,避免压垮探索)。
- **第 2 步(对症"射门冲出界")**:球门走廊——`|root_y| < 1.3m`(略宽于门宽 1.0)时把端线出界限放宽 0.6m,让"为射门合法冲进门区"不被罚;侧线和无关底线不变。
- **为什么两步一起**:只做第 1 步(硬罚出界)可能让策略变保守、不敢追球冲门(射门率塌)。第 2 步给"合法射门空间"对冲这个风险。
- **失败信号(研究红线)**:若 EXP17 射门率反而塌或 robot_to_ball 升 → 惩罚过重/走廊过窄,回退 weight。
- **判据**:`out_of_bounds 降向 ≤0.20` 且 `射门率守住 ~0.32`、`fell_over<0.1`、`pos_err<1.1`。

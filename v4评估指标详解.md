# v4 评估指标详解

> 2026-06-09 创建。本文件解释 v4 训练/评估中**用到的所有指标**:它们怎么算、阈值多少、好坏方向、为什么重要。
> 指标定义见 `scripts/readout_v4_metrics.py`,计算逻辑见 `src/mjlab/tasks/velocity/mdp/dribble_command.py`。
> 判读工具按三个维度分组打印:**自定位 / 踢球链 / 稳定性**。每个指标报 mean / median / min / max / n(采样点数)。

---

## 读数说明

- **采样点**:每个数来自训练日志的一次记录(通常每若干 iteration 记一次)。"末 100"指取最近 100 个记录点统计,反映当前水平而非全程平均。
- **mean vs median**:median(中位数)对偶发极端值更稳健。两者差距大说明波动剧烈。
- **方向**:每个指标标注 ↑越高越好 或 ↓越低越好。
- **护栏指标**:线B(踢球)训练时,自定位和稳定性指标当"护栏"用——它们只要**不退化**就算合格,不要求提升。

---

## 一、自定位 LOCALIZATION(线A 核心)

### `pos_err_m`(selfloc_pos_err_m) ↓越低越好
- **含义**:EKF 算出的机器人位置与真实位置(GT)的欧氏距离,单位米。
- **算法**:`sqrt((x_est - x_gt)² + (y_est - y_gt)²)`,对所有并行环境取平均。
- **目标**:< 0.8m。当前 EXP13/14 ≈ **0.98m**。
- **重要性**:线A 的命门指标。它决定机器人"知不知道自己在哪",直接影响能否朝正确方向带球射门。
- **注意**:评估时需 **warmup ~150 步**让 EKF 收敛后再统计,否则虚高(EKF 初始从宽先验起步)。

### `uniq_frac`(scan_uniq_frac) ↑越高越好
- **含义**:一段时间窗口内,23 个场地地标中**去重后总共看到了几个**的比例(0~1)。
- **算法**:窗口内每个地标"是否被看到过"取或,求和除以 23。
- **当前**:≈ 0.17(约 4/23)。
- **重要性**:衡量"视野信息量"。它低,说明机器人视野窄、看到的地标少,EKF/Kabsch 缺约束。这是主动视觉(让机器人转头多看)想提升的量。0.17 偏低,但 EKF 靠跨帧累积部分弥补了它。

---

## 二、踢球链 KICK-CHAIN(线B 核心,最终成品能力)

### `goal_rate`(进球率) ↑越高越好 ★最终主目标
- **含义**:把球真正踢**进球门**的 episode 比例(0~1)。
- **算法**:球进入球门区域(局部坐标 `goal_line_x-0.2 < x < goal_line_x+0.6` 且 `|y| < goal_half_width`)即记一次"进球",每 episode 只记一次(0→1 跳变)。
- **目标**:0.2。当前 EXP14 ≈ **0.10**。
- **重要性**:**整个 v4 的终极目标指标**。前面所有定位、走路、带球都是为它服务。
- **关键区分**:这是"球真进门",不同于下面的 `episode_success`(只要球到目标点附近就算)。goal_rate 更严格。

### `episode_success`(回合成功率) ↑越高越好
- **含义**:一个 episode 中,球是否**曾经到达目标点 0.5m 范围内**(0~1)。
- **算法**:`ball_to_target_error < success_threshold(0.5m)`,episode 内取历史最大(到过一次就算成功)。
- **当前**:≈ 0.39。
- **重要性**:比 goal_rate 宽松的"接近成功"指标。它高但 goal_rate 低,说明"球能带到目标附近,但临门一脚没踢进"——这正是当前卡点。

### `ball_to_tgt_err`(ball_to_target_error,球-目标距离) ↓越低越好
- **含义**:球当前位置到目标点的距离,单位米。
- **算法**:球 xy 与目标 xy 的欧氏距离,实时值。
- **当前**:≈ 2.6m,且在训练中持续下降(2.97→2.65)。
- **重要性**:衡量"球被带得离目标多近"。它持续降而 goal_rate 不涨,是"会接近、不会终结"的直接证据。

### `robot_to_ball`(robot_to_ball_error,机器人-球距离) ↓越低越好
- **含义**:机器人到球的距离,单位米。
- **重要性**:衡量"机器人能否贴近球"。太大说明追不上球;本项目稳定在 ~0.65m,说明追球没问题,瓶颈在踢不在追。

### `possession`(占球步数) ↑越高越好(需结合其他指标看)
- **含义**:一个 episode 中机器人"贴着球"(robot_to_ball < 0.5m)的累计步数。
- **重要性**:衡量"控球时间"。但要警惕:占球高 + 进球低 = 可能在"抱着球不射门"。需和 ball_speed、ball_stuck 一起看。

### `ball_path`(ball_path_length,球行进总路程) ↑通常越高越好
- **含义**:一个 episode 中球累计移动的路程,单位米。
- **重要性**:球被踢动得多说明机器人在积极推进。但路程长 + 离目标远 = 乱踢;要结合 ball_to_tgt_err 看是否朝目标推进。

### `ball_speed`(球速) — 适中为好
- **含义**:球的世界坐标 xy 速度,单位 m/s。
- **重要性**:核心"球动没动"信号。太低=粘球/不踢;过高也不一定好(踢飞)。是下面防作弊指标的基础。

### `ball_stuck_s`(ball_stuck_time,粘球时长) ↓越低越好【防作弊】
- **含义**:球几乎静止(ball_speed < 0.05 m/s)的累计秒数。
- **重要性**:**反作弊指标**。机器人可能学会"压着球不动"骗占球奖励。这个值高=球被卡住没在推进,是坏信号。

> 另有 `holding_time`(near<0.4m 且 slow<0.2m/s 的"占有"时长)等内部信号用于触发 reset 和反作弊,不在主判读表里。

---

## 三、稳定性 STABILITY(护栏)

### `fell_over`(摔倒率) ↓越低越好 ★关键护栏
- **含义**:机器人摔倒导致 episode 终止的比例。
- **重要性**:**最重要的护栏**。v4 六轮失败的核心症状就是摔倒暴涨(EXP5f 达 52)。EXP6 修复后降到 ~0,之后任何训练都必须守住它不反弹。当前 ≈ 0.05,健康。

### `out_of_bounds`(out_of_field_bounds,出界率) ↓越低越好
- **含义**:机器人或球越出场地边界导致终止的比例。
- **重要性**:次要护栏。一定比例(~0.2)可接受;暴涨说明策略乱跑或乱踢。

---

## 四、怎么综合判读(实战经验)

1. **先看护栏**:fell_over 和 pos_err 没退化,才有资格谈踢球进步。护栏破了,其他都是虚的。
2. **主目标看 goal_rate**:这是终极指标,其他踢球指标都是它的"过程解释"。
3. **诊断卡点用组合**:
   - `episode_success 高 + goal_rate 低` → 会接近不会终结(当前状态)。
   - `ball_to_tgt_err 降 + goal_rate 平` → 带球方向对,临门一脚缺。
   - `possession 高 + ball_stuck 高 + goal_rate 低` → 抱球作弊,reward 需修。
   - `robot_to_ball 大` → 连球都追不上(本项目未出现)。
4. **mean 和 median 一起看**:差距大说明个别环境拖累或暴走,需看 min/max 找极端情况。

---

## 附:指标-日志键名对照表

| 判读名 | 日志键名 | 维度 | 方向 |
|---|---|---|---|
| pos_err_m | Metrics/selfloc_pos_err_m | 定位 | ↓ |
| uniq_frac | Metrics/scan_uniq_frac | 定位 | ↑ |
| goal_rate | Metrics/dribble/goal_rate | 踢球 | ↑ ★ |
| episode_success | Metrics/dribble/episode_success | 踢球 | ↑ |
| possession | Metrics/dribble/possession | 踢球 | ↑* |
| ball_path | Metrics/dribble/ball_path_length | 踢球 | ↑* |
| ball_speed | Metrics/dribble/ball_speed | 踢球 | 适中 |
| ball_to_tgt_err | Metrics/dribble/ball_to_target_error | 踢球 | ↓ |
| ball_stuck_s | Metrics/dribble/ball_stuck_time | 踢球 | ↓ |
| robot_to_ball | Metrics/dribble/robot_to_ball_error | 踢球 | ↓ |
| fell_over | Episode_Termination/fell_over | 稳定 | ↓ ★ |
| out_of_bounds | Episode_Termination/out_of_field_bounds | 稳定 | ↓ |

(★=关键指标;*=需结合其他指标判断,不能孤立看)

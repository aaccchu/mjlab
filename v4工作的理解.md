**现状**
现在已经不是最早那个“机器人为什么不踢球”的阶段了。最早的问题已经查清楚：`05_realspec` 不踢球，第一层原因是相机设计把踢球用的 depth 分支一起改坏了；后来 dualcam 已经修了这层。再往后，Claude 证明了：如果从 `04_e2e` 同口径继承，踢球和进球能力是能保住的，真正卡住的是**纯视觉自定位**。

后面做了很多实验，结果很明确：让 CNN 直接从 RGB 图像回归机器人位置这条路走不通。无论是 `96x72`、`160x120` 还是 `384x288`，撤掉 GT 之后最后都会塌到大约 `6.5m`。所以现在已经放弃“单纯提分辨率 + 直接回归位姿”这条路。

现在的新主线是：**不要让神经网络直接猜机器人坐标，而是让它先看图找场地关键点，再用几何方法算出机器人位置。**  
通俗说，就是让机器人在图像里找“场地角点、罚球区角、中线、中圈、球门柱”等点；这些点在真实场地地图上的坐标是已知的，再结合 depth，把这些点从图像抬成 3D 点，然后用几何配准算出机器人在哪里。

这个思路本身已经被验证过：如果关键点检测是准的，几何链能把位姿算到厘米级误差。这说明**数学和几何这部分是通的**。现在唯一没解决的是：怎么让网络稳定、准确地学会找这些关键点。

**刚刚发生了什么**
Claude 试了三种用 RL reward 学关键点的方法：

- `EXP5c`：奖励设计有漏洞，机器人学会“不看场地”，因为看不见关键点时误差反而变小，等于钻空子。
- `EXP5d`：修了“不看场地”的漏洞，机器人确实会保持看到一些关键点，但像素检测精度学不动，定位还是 `6-7m`。
- `EXP5e`：把 reward 调宽，让梯度回来，像素误差确实降了一些，但副作用是机器人本体动作崩了，摔倒暴涨，不踢球。

这说明一件事：**关键点检测不适合继续靠 PPO 的标量 reward 学。**  
它本质上是一个视觉监督学习问题，应该像训练检测器那样，直接告诉网络“这个关键点应该在图像这个位置”，而不是通过“你踢球/定位好不好”这种间接奖励慢慢猜。

**下一步在做什么**
下一步是 `EXP5f`：把关键点检测从 RL reward 里拆出来，改成**监督辅助 loss**。

通俗说就是：

- 训练时，模拟器知道每个场地关键点在图像里的正确位置。
- 把这些正确位置作为标签存下来。
- 网络输出自己预测的关键点位置。
- 直接算“预测点和正确点差多少像素”，用这个差值训练检测头。
- 这个检测 loss 不再塞进踢球奖励里，避免它和走路、踢球、保持平衡抢梯度。
- PPO 继续负责运动和踢球，监督 loss 负责教视觉检测。

技术上，Claude 提出三条路。我建议的是变体 `A'`：把关键点标签作为“非策略 observation”存进 rollout buffer，但不喂给 actor/critic。然后写一个本地 `KeypointAuxPPO`，在 PPO 更新时额外加这个监督 loss。这样既能保证标签和每个 rollout 样本对齐，又不需要直接改 `.venv/site-packages/rsl_rl`。

**成功几率**
我分三层看：

- 几何定位这层，成功概率高。因为已经验证过：关键点准的时候，Kabsch/几何链能到厘米级。
- 关键点检测这层，中等偏高。因为现在不再靠稀疏 reward，而是改成直接监督，问题会简单很多。主要风险是接入实现复杂、标签不能误进策略输入、可见性和遮挡处理要干净。
- 最终“纯视觉踢球”这层，中等。即使定位模块做好，还要接回 e2e，保证机器人能一边用视觉定位，一边追球、踢球、进球、不摔倒。这一步还有工程风险。

我的判断是：  
**方向现在是对的，已经从反复调 reward/分辨率，转到更靠谱的视觉几何方案。下一步 `EXP5f` 是目前最值得投入的实现。**

但它不是“小改就成功”的阶段。更像是进入了一个关键工程关口：如果 `EXP5f` 能把关键点检测训练稳定，后面才有机会做出真正的 v4 正式产物；如果 `EXP5f` 也失败，就要考虑离线预训练检测器、冻结视觉模块、或者再上 MCL 做时序稳定。当前离正式交付还没到最后一公里，但已经找到了比之前更像正解的路线。

---

# 续写（2026-06-09 更新：EXP5f 之后发生的事）

> 上面写到 EXP5f 为止。从那之后又跑了 EXP6 到 EXP14 一连串实验，主线发生了**两次重要转折**，定位问题**已经基本解决**。下面按“发生了什么 → 为什么 → 怎么解决的”讲清楚，尽量通俗。

## 第一次转折：发现真正的病根不是“奖励设计”，而是“网络结构”

EXP5f 的思路（把关键点检测从 reward 拆成监督 loss）方向是对的，但实测又失败了：摔倒还是暴涨。Claude 顺着这条线深挖，**找到了 EXP1 到 EXP5f 反复失败的共同病根**：

**机器人的“看”和“走”用的是同一个神经网络主干（共享 trunk）。** 这意味着只要往“看得更准”这个方向施加任何学习压力——不管是 reward 还是监督 loss——这股力都会顺着共享的网络传到“走路”那部分，把已经学好的步态搅乱。所以现象一直是：视觉一开始学，机器人就开始摔。这不是调参能解决的，是结构问题。

**证据很硬**：摔倒率随“视觉学习压力”单调上升（EXP5d≈1 → EXP5e 37 → EXP5f 52）。

## 解决办法（EXP6）：把“感知”从动作里搬出来，变成“观测”

这是个关键的设计反转，通俗说：

- **以前**：让神经网络一边走路、一边“顺手”把自己在哪也猜出来——猜位置是它要做的**动作之一**。结果猜位置这件事干扰了走路。
- **EXP6 之后**：位置不再让网络去“猜”。而是用固定的几何公式**先算好**机器人在哪，把算出来的结果当成一条**已知信息（观测）直接喂给网络**，就像告诉它“你现在大概在这个坐标”。网络只管用这个信息去走路、踢球，不用自己分心去算。

**效果立竿见影**：摔倒率从 52 直接掉到 0。困扰了六轮实验的结构病根，一次解决。**这是整个 v4 的第一个里程碑。**

## 第二次转折：定位精度卡在 1.5-2m，发现“拼接多帧”是错的方法

架构修好后，新问题浮现：用几何方法算出来的位置**不够准**，误差卡在 1.5-2m（目标要 <0.8m）。

原因查清楚了：机器人头部相机视野窄，**一帧画面通常只能看到 23 个场地地标里的 ~4 个**。地标太少，几何配准（Kabsch）就“欠定”——好比只看到墙角的两条线，没法唯一确定自己站在房间哪个位置。

Claude 先试了“把连续好几帧看到的点拼到一起再算”（EXP7-11），但**几乎没用**：因为机器人朝一个方向走时，每帧看到的还是那同样几个点，拼起来信息没增加。

**这里 Claude 做了一件关键的事：上 GitHub 和论文库调研 RoboCup（机器人足球世界赛）几十年的经验。** 查到的结论一针见血：

> **永远不要靠单帧或拼帧做几何配准定位。要用 EKF/UKF 这类“递归滤波器”，让信息真正跨时间累积。**

通俗解释 EKF 和“拼帧”的区别：
- **拼帧**（之前的错法）：把好几张照片的点堆在一张纸上一次性算，照片角度都差不多，堆了也白堆。
- **EKF**（正解）：维护一个“我在哪”的概率猜测。每走一步先用速度推算“我大概挪到了哪”，再用这一帧哪怕只看到 1 个地标去**修正**这个猜测。信息一步步累积、越来越准——像滚雪球，而不是堆照片。

## 解决办法（EXP13）：把定位换成 EKF 递归滤波

Claude 据此重写了定位模块（EXP12 离线验证几何正确 → EXP13 接入训练）。

**结果达标**：定位误差 **pos_err 降到 0.98m**（之前卡 1.5-2m），同时摔倒率保持在 0.06 的低位、不踢球能力也没退化。**这是 v4 的第二个里程碑：纯视觉自定位这条主线，基本打通了。**

> 注：这里有个评估细节叫 **warmup（预热）**。EKF 刚开始不知道机器人在哪，要走几十步才收敛到稳态。所以评估时要先空跑 ~150 步让它收敛，这些步不计分，只统计收敛后的真实表现——否则会把“还没收敛”的虚高误差冤枉算到模型头上。

## 现在在做什么（EXP14，进行中）

定位（称为“线A”）成了之后，重心转回**踢球进球（称为“线B”）**——这才是最终要的成品能力。

当前 EXP14 在练“临门一脚”：让球在机器人脚边出生、奖励它把球快速踢向球门。同时用了**防遗忘设计**（一半时间练踢球、一半时间保持完整任务），防止“学了踢球又把定位和走路忘了”——这个“学新忘旧”的毛病之前 codex 和 Claude 都踩过坑。

**EXP14 实时状态（跑到 1078/2000）**：
- 进球率 goal_rate：~0.10（比基线 0.08 略涨，但还没到目标 0.2）
- 定位 pos_err：0.97m（**护栏守住，没退化**）✅
- 摔倒率：0.06（**护栏守住**）✅
- 球离目标距离：从 2.97m 持续降到 2.65m（球越来越会被带向球门）

**怎么理解**：球能被带到离门 ~2.6m，但**最后那一脚射门进球还没稳定学会**。这和 codex 观察到的“射门退化”是同一个硬骨头——带球接近不难，终结射门难。要看后半程能否突破，若不行，下一步要专门给“射门”做奖励塑形。

## 整体进度判断（截至 2026-06-09）

| 能力 | 状态 |
|---|---|
| 修好“看/走互相干扰”的结构病根 | ✅ 已解决（EXP6） |
| 纯视觉自定位精度达标（<1m） | ✅ 基本达成（EXP13，0.98m） |
| 走路稳定不摔 | ✅ 稳固（摔倒率 ~0.05） |
| **踢球进球率达标** | 🟡 **进行中，当前主攻点**（EXP14，goal_rate ~0.10 vs 目标 0.2） |

**一句话总结**：v4 已经翻过了两座大山——结构病根和视觉定位都解决了，这是真正的突破。现在卡在最后的“临门一脚”，这是收尾阶段的硬骨头，但已经不是方向不明的探索，而是明确目标下的攻坚。

---

# 关键术语表

> 上文为了通俗用了不少比喻，这里把涉及的术语正式列出：每条给「全称 / 通俗解释 / 在本项目里具体指什么」，方便对照和检索。

## 强化学习基础

- **RL（Reinforcement Learning，强化学习）**：让机器人通过“试错 + 奖励”自己学会做事的方法。做得好给正分、做得差给负分，它自己摸索怎么拿高分。本项目用 RL 教机器人走路、追球、踢球。
- **PPO（Proximal Policy Optimization，近端策略优化）**：目前最主流的一种 RL 算法，特点是更新稳、不容易训崩。本项目的“走路 + 踢球”策略就是用 PPO 训练的。
- **reward（奖励）**：给机器人行为打的分，是 RL 的指挥棒。奖励设计错了，机器人会“钻空子”拿高分却不干正事（EXP5c 就是反例：它学会“不看场地”来骗分）。
- **policy（策略）/ actor / critic**：
  - **policy（策略）**：机器人的“大脑决策函数”——输入它看到/知道的信息，输出该做什么动作。
  - **actor（演员）**：策略里负责“输出动作”的网络。
  - **critic（评论家）**：训练时负责“评估这个动作好不好”的网络，只在训练时用，部署时不需要。
- **observation（观测）**：喂给策略的输入信息（机器人自身姿态、球的位置、算出来的自身坐标等）。**EXP6 的关键反转就是：把“自身位置”从“让网络猜的动作”改成“直接告诉网络的观测”。**
- **rollout（回合采样 / 轨迹）**：让机器人按当前策略实际跑一段，把每一步的“观测-动作-奖励”记录下来，用这批数据更新策略。
- **trunk（主干网络）**：神经网络中多个任务共享的那部分。**v4 六轮失败的病根就是“看”和“走”共享同一个 trunk，导致互相干扰。**

## 自定位相关（线A）

- **自定位（self-localization）**：机器人靠自己的传感器（这里是相机）算出“我现在在球场的哪个坐标、朝哪个方向”。
- **关键点 / 地标（keypoint / landmark）**：球场上位置已知的显著点——场地角、罚球区角、中圈、球门柱等，本项目共 23 个，它们在地图上的坐标是已知的。
- **Kabsch（Kabsch 算法）**：一种几何配准方法。已知“一组点在机器人视角下的坐标”和“同一组点在地图上的真实坐标”，它能算出机器人相对地图的位姿。**问题在于看到的点太少（4/23）时它会“欠定”——算不准。**
- **欠定（underdetermined）**：约束不够、解不唯一。好比只看到墙角两条线，没法唯一确定自己站在房间哪里。
- **depth（深度 / 深度图）**：相机每个像素到物体的距离。本项目用它把图像里的关键点“抬升”成 3D 坐标，再交给 Kabsch。
- **EKF / UKF（Extended / Unscented Kalman Filter，扩展 / 无迹卡尔曼滤波）**：一种“递归滤波器”。它维护一个“我在哪”的概率估计，每步先用速度**预测**新位置、再用观测到的地标**修正**。信息跨时间累积，越来越准。**这是 v4 定位达标（0.98m）的关键方法，取代了之前的“拼帧 + 单次 Kabsch”。**
  - **预测步 / 更新步**：EKF 的两个环节。预测步用运动（里程计/速度）推算位置并增大不确定度；更新步用新观测拉回、减小不确定度。
  - **里程计（odometry）**：靠自身运动（速度、步态、IMU）推算“我挪动了多少”，是 EKF 预测步的输入。
- **MCL（Monte Carlo Localization，蒙特卡洛定位 / 粒子滤波）**：另一类定位方法，用大量“粒子”代表可能位置。适合处理**对称球场的多解二义性**（备选方案，目前未用）。
- **pos_err（position error，定位误差）**：算出来的位置和真实位置差多少米，是线A 的核心评分指标。目标 <0.8m，当前 0.98m。
- **warmup（预热）**：EKF 刚 reset 时不知道机器人在哪，要走几十步才收敛。评估时先空跑 ~150 步让它收敛、这些步不计分，只统计收敛后的稳态——否则会冤枉模型。
- **uniq_frac（unique fraction，唯一地标覆盖率）**：一段时间内总共看到了 23 个地标里的几个（去重）。衡量“视野信息够不够”，越高定位越有底气。

## 训练方法 / 工程

- **监督 loss（supervised loss，监督学习损失）**：直接告诉网络“正确答案是什么”，算预测和答案的差距来训练。比 RL 的“间接奖励”学得更快更准。EXP5f 曾想用它教关键点检测。
- **GT（Ground Truth，真值）**：模拟器知道的“标准答案”（如机器人真实坐标）。训练时可临时用作“拐杖”，但最终必须撤掉、只靠视觉——v4 早期的失败多发生在“撤掉 GT”之后。
- **e2e（end-to-end，端到端）**：从“相机原始输入”直接到“控制输出”整条链路一起训练/运行，不依赖外部喂答案。是最终交付形态。
- **goal_rate（进球率）**：踢球进门的比例，是线B（最终成品能力）的核心指标。目标 0.2，当前 ~0.10。
- **防遗忘 / 灾难性遗忘（catastrophic forgetting）**：学了新技能（踢球）把旧技能（定位、走路）忘掉的现象。本项目用“一半时间练踢球、一半保持完整任务”来防它。
- **teacher distillation（教师蒸馏）**：先分别训练好“单项专家”（如只会定位的、只会踢球的），再把它们的本事“蒸馏”进一个统一网络，避免互相覆盖。是解决踢球/定位冲突的备选方案。
- **线A / 线B（本项目内部叫法）**：线A = 自定位（已基本达标）；线B = 踢球进球链（当前主攻）。两条线尽量解耦推进。
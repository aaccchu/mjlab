# v4 越界(Out of Bounds)处理调研

> 2026-06-09。起因:EXP16 训练中 out_of_bounds 涨到 0.36,深挖原因 → 调研足球越界规则 +
> RL 工程处理。核心结论:**我们把"越界"误标成 time_out=True 被 PPO bootstrap,等于告诉算法
> "出界免罚",这是语义错误而非调参问题。** 本文存档调研结果,供后续修复参考。

---

## 0. 最重要的结论(先看这个)

**out_of_bounds 涨到 0.36 的机制性根因 = time_out 语义错误。**

代码证据链(已核实):
1. `env_cfgs.py:425` — `out_of_field_bounds` 设 `time_out=True`。
2. `vecenv_wrapper.py:83` — `time_out=True` 的项进 `extras["time_outs"]`(truncated 桶)。
3. `rsl_rl/algorithms/ppo.py:150-155` — 对 time_outs 的 step:
   `rewards += gamma * values * time_outs` —— **把"继续下去的价值"补回奖励**(bootstrap)。

=> 每次出界,PPO 给机器人补上 `γ·V(出界后状态)` 的奖励,等于明示"出界和正常继续一样有价值"。
   机器人因此**完全不在乎出界**。这不是权重问题,是把"任务失败"误标成了"时间截断"。

理论依据(高可信,有文献):
- **Pardo et al. 2017, "Time Limits in RL"** (arXiv:1712.00378):判据是"这个结束是不是真实 MDP 的
  吸收态"。纯**时间到点**→ 该 bootstrap(truncation);**真实失败/目标达成**→ 不该 bootstrap(termination)。
- **Gymnasium** 正式区分 `terminated`(真失败/成功,不 bootstrap)vs `truncated`(外部时限,bootstrap)。
- 越界 = 离开有效游戏区 = **任务失败**,不是时间截断 => 应当 `terminated`,不该 bootstrap。**无争议**。

---

## 1. EXP16 现象的数据佐证(因果链)
- out_of_field_bounds 判定的是**机器人 root 出界**(|x|>10.7 或 |y|>6.7, margin=0.3),**与球无关**
  (terminations.py:71-89)。纠正了"球弹飞出界"的早期误判。
- 时间序列:out_bnd 0.28→0.36,与 ball_speed_peak(2.57→2.94)、robot_to_ball(0.75→0.92)同步上升。
- 相关性:`out_bnd vs goal_rate +0.514`(强!进球越多出界越多)、`vs target_is_goal +0.242`
  (球门 episode 更易出界)、`vs possession −0.292`(出界 episode 不是抱球磨蹭)。
- 因果:学会朝球门(+x)大力射门 → 射门时身体前冲 → 球门 x=11、出界线 x=10.7 只差 0.3m →
  机器人射门冲过出界线 → time_out 不罚 → 无动机收脚 → out_bnd 随射门能力一起涨。

---

## 2. 足球规则背景(RoboCup / FIFA)

### FIFA 几何定义【规则事实】
- 球出界 = **整个球**(the whole of the ball)完全越过边线/底线。压线/线上空 = 仍在场内。判据是**球最远点**过线,非球心。
- 进球同理:整个球越过球门线(门柱间、横梁下)。
- 恢复:出边线→掷界外球;出底线非进球→进攻方碰=球门球,防守方碰=角球。

### RoboCup 各组【规则事实,中高可信】
- **SPL(NAO)**:球出界裁判放回参考点;机器人离场=penalize(移出场 N 秒再放回)。仓库:RoboCup-SPL/Rules。
- **Humanoid League**:移植 FIFA,有 throw-in/goal kick/corner 对应,裁判手动放球;机器人摔/卡/越界有 removal penalty。
- **3D 仿真组(最接近我们)**:自动裁判检测球越线→**beam(瞬移)回场内**规则位置 + 摆位附近球员;
  game state 切 KickIn/CornerKick/GoalKick;游戏**不终止**只切状态。机器人越界/拥挤→beam 回合法位。
  reset 用 beam(直接设位姿)= 我们 RL 的 reset 写 qpos。**无物理墙**(中型组 MSL 才有墙)。

### 进球 vs 底线出界几何区分
- 底线被门柱分段:球越底线时,越线点 |y|<门宽半(我们=1)且横梁下→进球;否则→底线出界。
- 我们无横梁约束 => 简化为球心 x 越 ±11 时看 |y|<1(进球)还是 ≥1(出界)。

---

## 3. RL 边界处理方法菜单

| 方法 | 思路 | 优劣 | 改法 |
|---|---|---|---|
| **1.软惩罚shaping** | 越软边界按深度给连续负reward,不终止 | +平滑不浪费episode不破坏带球;−允许短暂越界需硬兜底 | `r-=k·max(0,|x|-x_soft)` |
| **2.硬终止+惩罚** | 越硬边界→terminated(不bootstrap)+一次性负奖 | +信号最强语义正确;−过紧会保守 | `reset=True;time_out=False;r-=C_oob` |
| **3.球门端缓冲区** | 底线在门宽内向外延伸缓冲不判越界 | +允许追球冲门(对症);−需分段几何 | 出界判据按y分段,门口x阈值放宽 |
| **4.球vs机器人分离** | 两者用不同done/reward | +贴近真实;−复杂 | 机器人越界软shaping;球越界terminated+小负奖 |
| **5.射门后留场内** | 罚root朝边界速度+站稳奖励 | +直接打中前冲;−调权重 | `r-=k_v·max(0,v_toward_boundary)` |
| **6.物理墙** | 边缘加碰撞体 | +绝对不出界;−撞墙怪行为不真实,sim-to-real差 | 加invisible wall几何 |

### 文献做法【部分推断】
- DeepMind bipedal soccer(2304.13653):球/机器人出界或进球→episode结束重置;密集shaping
  (close-to-ball/vel-to-ball/ball-to-goal);**不专门重罚踢球出界**——出界自然失去未来正奖励,机会成本即惩罚。
- "踢球出界给显式负奖励"非主流;主流是隐式(失去未来奖励)。但稀疏奖励太稀时,加**小**显式球出界负奖是合理加速手段。
- 射门后冲出界:用踢球后平衡/站稳奖励 + base velocity 惩罚 + 球门后 run-off 缓冲区。

---

## 4. 最值得借鉴的 3 个做法(排序,可落地)

### ★第1优先:把越界从 truncation 改成 termination(语义修正,根因,改动最小)
现在 time_out=True 让PPO bootstrap出界后价值=明示"出界免罚"。依据 Pardo2017+Gymnasium,越界是任务失败不该bootstrap。
```python
# terminations: out_of_bounds 改 time_out=False(进terminated桶,不bootstrap)
# rewards: 终止那步加一次性负奖励
def rew_out_of_bounds(env):
    return -C_oob * out_of_bounds_mask.float()   # C_oob先取~单次进球奖0.5~1倍,勿过大压垮探索
```
单这步就能压下0.36,因策略第一次"感到"出界有代价。

### ★第2优先:球门端缓冲区,底线出界与球门解耦(对症射门冲出界)
机器人追球射门前冲越x=±10.7被判出界。让它门口能合法冲出:
```python
def terminated_oob(env):
    side_out = root_y.abs() > Y_HARD                       # 侧边线不变
    in_corridor = root_y.abs() < (GOAL_HALF_W + 0.3)       # 门口走廊略宽于门
    x_limit = where(in_corridor, 11.0 + GOAL_BUFFER, 10.7) # 门口放宽到11+buffer
    return side_out | (root_x.abs() > x_limit)
```
"为射门冲进门区"不再被罚,只罚真跑偏出侧线/无关底线。

### ★第3优先:朝边界速度惩罚 + 软边界shaping(防第1步变太保守)
只硬终止易让策略缩场中央不敢追球,加软区梯度引导:
```python
def rew_boundary_soft(env):  # 越界越深越疼,权重小0.1~0.5
    return -((root_x.abs()-X_SOFT).clamp(min=0)+(root_y.abs()-Y_SOFT).clamp(min=0))
def rew_vel_toward_boundary(env):  # 专治射门飞出:近边界时罚朝外速度
    v_out=(root_vx*sign(root_x)).clamp(min=0)+(root_vy*sign(root_y)).clamp(min=0)
    near=(root_x.abs()>X_SOFT)|(root_y.abs()>Y_SOFT)
    return -(v_out*near.float())
```

**落地顺序**:先做第1步(语义修正,bug级,最小改动最大收益)→ 看oob降但追球意愿是否受损 →
叠第2步解耦球门 → 若变保守再加第3步软引导。C_oob/权重都从小开始grid,勿一上来压垮探索。

---

## 5. 关键链接
规则: FIFA Ball in/out of play、RoboCup-SPL/Rules、RoboCup-Humanoid-TC/Rules、ssim.robocup.org/3d-rules
RL: Pardo2017 arXiv:1712.00378(truncation vs termination)、Gymnasium handling time limits、
   DeepMind 2304.13653、dm_control soccer、Striker 2512.06571、Humanoid soccer 2602.05310、legged_gym

## 6. 待决策(已问用户,选了"后面再看"具体修法;本轮先存调研)
- 用户倾向:先调研背景(本文)。是否实施第1/2/3步修复,EXP16跑完看结果再定。
- 注意:第1步(termination语义)是几乎确定该修的bug级问题,但要观察是否让策略变保守(不敢追球)。
  建议与第2步(球门缓冲区)一起上,避免"修了出界但压低射门率"。
